# Generated by zipx. Do not edit. Run 'sbt zipxWorkflowGenerate' to regenerate.
name: CI
"on":
  push:
    branches:
      - main
    tags:
      - v[0-9]+.[0-9]+.[0-9]+
  pull_request: null
  workflow_dispatch: null
concurrency:
  group: CI-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/') }}
jobs:
  verify-gate:
    name: verify-gate
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && !startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: read
      pull-requests: read
    env:
      PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/target/ms-playwright
    outputs:
      run: ${{ steps.check.outputs.run }}
    steps:
      - name: Skip Verify after merged PR push
        id: check
        run: |
          # Commits landed by merging/squashing a PR are associated with that PR via the API.
          prs=$(gh api "repos/${{ github.repository }}/commits/${{ github.sha }}/pulls" \
            --jq "[.[] | select(.merged_at != null and .base.ref == \"${{ github.ref_name }}\")] | length")
          if [ "$prs" -gt 0 ]; then
            echo "Merged PR push, skipping redundant Verify (already ran on the PR)"
            echo "run=false" >> "$GITHUB_OUTPUT"
          else
            echo "run=true" >> "$GITHUB_OUTPUT"
          fi
        env:
          GH_TOKEN: ${{ github.token }}
  cache-rehydrate:
    name: cache-rehydrate
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: needs.verify-gate.result == 'success' && needs.verify-gate.outputs.run == 'false'
    env:
      PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/target/ms-playwright
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: cache-rehydrate
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: ""
      - name: Cache Playwright apt packages
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/chekhov-apt-archives
          key: ${{ runner.os }}-chekhov-apt-${{ hashFiles('package-lock.json') }}
          restore-keys: ${{ runner.os }}-chekhov-apt-
      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24"
          cache: npm
      - name: npm ci
        run: npm ci
      - name: npm ci (vite fixture)
        run: npm ci --prefix examples/vite-fixture
      - name: npm ci (ascent fixture)
        run: npm ci --prefix examples/ascent-fixture
      - name: Install Playwright browsers
        run: |
          set -euo pipefail
          apt_mirror="${HOME}/.cache/chekhov-apt-archives"
          mkdir -p "${apt_mirror}"
          sudo mkdir -p /var/cache/apt/archives/partial
          if ls "${apt_mirror}"/*.deb >/dev/null 2>&1; then
            echo "Seeding apt archives from ${apt_mirror} ($(ls -1 "${apt_mirror}"/*.deb | wc -l | tr -d ' ') debs)"
            sudo cp -n "${apt_mirror}"/*.deb /var/cache/apt/archives/ || true
          fi
          mkdir -p "${PLAYWRIGHT_BROWSERS_PATH}"
          chmod +x ./scripts/install-browsers.sh
          ./scripts/install-browsers.sh chromium chromium-headless-shell firefox webkit
          if ls /var/cache/apt/archives/*.deb >/dev/null 2>&1; then
            sudo cp -n /var/cache/apt/archives/*.deb "${apt_mirror}/" || true
            sudo chown -R "$(id -u):$(id -g)" "${apt_mirror}"
            echo "Apt mirror now has $(ls -1 "${apt_mirror}"/*.deb | wc -l | tr -d ' ') debs"
          fi
          echo "Playwright $(node -p "require('playwright/package.json').version") browsers ready"

      - name: cache-rehydrate
        run: sbt 'compile'
  test:
    name: test
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    env:
      PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/target/ms-playwright
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: test
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: ""
      - name: Cache Playwright apt packages
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/chekhov-apt-archives
          key: ${{ runner.os }}-chekhov-apt-${{ hashFiles('package-lock.json') }}
          restore-keys: ${{ runner.os }}-chekhov-apt-
      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24"
          cache: npm
      - name: npm ci
        run: npm ci
      - name: npm ci (vite fixture)
        run: npm ci --prefix examples/vite-fixture
      - name: npm ci (ascent fixture)
        run: npm ci --prefix examples/ascent-fixture
      - name: Install Playwright browsers
        run: |
          set -euo pipefail
          apt_mirror="${HOME}/.cache/chekhov-apt-archives"
          mkdir -p "${apt_mirror}"
          sudo mkdir -p /var/cache/apt/archives/partial
          if ls "${apt_mirror}"/*.deb >/dev/null 2>&1; then
            echo "Seeding apt archives from ${apt_mirror} ($(ls -1 "${apt_mirror}"/*.deb | wc -l | tr -d ' ') debs)"
            sudo cp -n "${apt_mirror}"/*.deb /var/cache/apt/archives/ || true
          fi
          mkdir -p "${PLAYWRIGHT_BROWSERS_PATH}"
          chmod +x ./scripts/install-browsers.sh
          ./scripts/install-browsers.sh chromium chromium-headless-shell firefox webkit
          if ls /var/cache/apt/archives/*.deb >/dev/null 2>&1; then
            sudo cp -n /var/cache/apt/archives/*.deb "${apt_mirror}/" || true
            sudo chown -R "$(id -u):$(id -g)" "${apt_mirror}"
            echo "Apt mirror now has $(ls -1 "${apt_mirror}"/*.deb | wc -l | tr -d ' ') debs"
          fi
          echo "Playwright $(node -p "require('playwright/package.json').version") browsers ready"

      - name: test
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; core/testFull; protocol/testFull; driver/testFull; jsenv/testFull; zio-test/testFull; docs/testFull; jsenv-smoke/testFull; dom/testFull'
          else
            sbt 'core/testFull; protocol/testFull; driver/testFull; jsenv/testFull; zio-test/testFull; docs/testFull; jsenv-smoke/testFull; dom/testFull'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  fmt:
    name: fmt
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    env:
      PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/target/ms-playwright
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: fmt
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: ""
      - name: fmt
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; scalafmtCheckAll'
          else
            sbt 'scalafmtCheckAll'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  workflow-check:
    name: workflow-check
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    env:
      PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/target/ms-playwright
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: workflow-check
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: ""
      - name: workflow-check
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; zipxWorkflowCheck'
          else
            sbt 'zipxWorkflowCheck'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  advisories:
    name: advisories
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    env:
      PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/target/ms-playwright
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: advisories
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: ""
      - name: advisories
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; zipxAdvisoryCheck'
          else
            sbt 'zipxAdvisoryCheck'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  publish:
    name: publish
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    env:
      PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/target/ms-playwright
      PGP_KEY_HEX: ${{ secrets.PGP_KEY_HEX }}
      PGP_PASSPHRASE: ${{ secrets.PGP_PASSPHRASE }}
      SONATYPE_PASSWORD: ${{ secrets.SONATYPE_PASSWORD }}
      SONATYPE_USERNAME: ${{ secrets.SONATYPE_USERNAME }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: publish
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: ""
      - name: Import signing key
        run: |
          mkdir -p ~/.gnupg && chmod 700 ~/.gnupg
          echo "allow-loopback-pinentry" >> ~/.gnupg/gpg-agent.conf
          echo "pinentry-mode loopback"   >> ~/.gnupg/gpg.conf
          gpgconf --kill gpg-agent || true
          echo "$PGP_SECRET" | base64 --decode | gpg --batch --import
        env:
          PGP_SECRET: ${{ secrets.PGP_SECRET }}
      - name: publish
        run: sbt 'core/publishSigned; dom/publishSigned; ascent/publishSigned; protocol/publishSigned; driver/publishSigned; jsenv/publishSigned; zio-test/publishSigned; sbt-chekhov/publishSigned; sonaRelease'
  docs:
    name: docs
    if: (startsWith(github.ref, 'refs/tags/v')) || (github.event_name == 'workflow_dispatch')
    permissions:
      contents: read
      pages: write
      id-token: write
    uses: early-effect/.github/.github/workflows/specular-docs.yml@main
    with:
      sbt-project: docs
